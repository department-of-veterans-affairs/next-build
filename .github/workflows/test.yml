name: CI - Test
on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'

jobs:
  test:
    name: Setup
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2-beta
        with:
<<<<<<< HEAD
          node-version: '16.13.0'
=======
          node-version: '16.6.2'
>>>>>>> 40ec180 (VA000-Squash commits)
          check-latest: true

      - name: Install Npm Dependencies
        run: yarn install

      - name: Run Lint
        run: yarn lint

      - name: Run tests with jest
        run: yarn test:ci

      # - name: Cypress.io
      #   uses: cypress-io/github-action@v2
      #   with:
      #     start: yarn start
      #     wait-on: 'http://localhost:3000'
